# Support Platform
[🌐 README in english](README.md)


Мультиплатформенная система поддержки пользователей с единым core-движком  
(на данный момент реализована интеграция с Telegram).

Проект построен по принципам **Hexagonal / Clean Architecture**:
- вся бизнес-логика находится в `core`
- платформы (Telegram, Discord итд) подключаются через адаптеры
- UI и конфигурация вынесены отдельно

## Содержание

- [Возможности](#возможности)
- [Требования](#требования)
- [Как использовать](#как-использовать)
- [Dev запуск](#dev-запуск)
- [Архитектура](#архитектура)
- [Основная идея](#основная-идея)
- [Как добавить новую платформу](#как-добавить-новую-платформу)
- [Конфигурация и web панель](#конфигурация-и-web-панель)
- [Roadmap](#roadmap)
---

## Возможности

- тикеты поддержки (один открытый тикет на пользователя)
- rate limit сообщений
- бан пользователей
- приветственное сообщение при первом обращении
- возможность добавить header и footer ответам поддержки
- поддержка вложений
- ответы оператора -> пользователю
- веб-интерфейс для редактирования конфига `application.yml`
- архитектура, готовая к подключению новых платформ

## Требования

* Java 21
* PostgreSQL или MySQL
* Maven (для разработки)

## Как использовать
Скачать .jar + .yml из [releases](https://github.com/Lar000ki/support-bot/releases) и положить в одну директорию
```bash
java -jar target/support-platform-<актуальная версия>.jar
```
## Dev запуск

### Запуск

```bash
mvn spring-boot:run
```
### Сборка и запуск jar (jar требует .yml в своей директории)

```bash
mvn clean package
```
```bash
java -jar target/support-platform-<актуальная версия>.jar
```

## Архитектура
<details>
<summary><b>command</b></summary>
<code>tree -I 'target|.git|.idea|*.class|test' --dirsfirst</code>
</details>

```
├── src
│   └── main
│       ├── java
│       │   └── com
│       │       └── bob
│       │           └── support_platform
│       │               ├── config
│       │               │   ├── SupportConfig.java
│       │               │   └── SupportProperties.java
│       │               ├── core
│       │               │   ├── dto
│       │               │   │   ├── AdminReplyContext.java
│       │               │   │   └── CoreCommand.java
│       │               │   ├── interfaces
│       │               │   │   ├── CoreResponse.java
│       │               │   │   └── PlatformMessage.java
│       │               │   ├── model
│       │               │   │   ├── CommandScope.java
│       │               │   │   ├── PlatformType.java
│       │               │   │   ├── Ticket.java
│       │               │   │   ├── TicketStatus.java
│       │               │   │   └── User.java
│       │               │   ├── service
│       │               │   │   ├── RateLimitExceededException.java
│       │               │   │   ├── RateLimitService.java
│       │               │   │   ├── SupportService.java
│       │               │   │   ├── TextService.java
│       │               │   │   ├── TicketService.java
│       │               │   │   ├── UserBannedException.java
│       │               │   │   └── UserService.java
│       │               │   ├── CoreCommandProcessor.java
│       │               │   ├── CoreCommandType.java
│       │               │   ├── CoreSupportProcessor.java
│       │               │   └── PlatformConfigResolver.java
│       │               ├── platform
│       │               │   ├── discord
│       │               │   └── telegram
│       │               │       ├── adapter
│       │               │       │   ├── TelegramCommandAdapter.java
│       │               │       │   ├── TelegramMessageAdapter.java
│       │               │       │   └── TelegramPlatformMessage.java
│       │               │       ├── TelegramBot.java
│       │               │       ├── TelegramBotConfig.java
│       │               │       ├── TelegramProperties.java
│       │               │       ├── TelegramSender.java
│       │               │       └── TelegramUpdateHandler.java
│       │               ├── repository
│       │               │   ├── TicketRepository.java
│       │               │   └── UserRepository.java
│       │               ├── web
│       │               │   ├── ConfigController.java
│       │               │   ├── SupportConfigDto.java
│       │               │   ├── UiController.java
│       │               │   ├── WebSecurityConfig.java
│       │               │   └── YamlConfigService.java
│       │               └── SupportPlatformApplication.java
│       └── resources
│           ├── static
│           │   └── app.js
│           └── templates
│               └── index.html
├── application.yml
└── pom.xml

```

## Основная идея

**Core ничего не знает о платформах (telegram, discord и тд)**

Core:
- принимает абстрактные сообщения (`PlatformMessage`)
- возвращает намерения (`CoreResponse`)
- не отправляет сообщения сам

Платформы:
- определяют контекст (user / admin / command)
- адаптируют нативные сообщения
- выполняют `CoreResponse`

## Как добавить новую платформу

**Чтобы добавить новую платформу, необходимо реализовать следующие элементы:**

### 1: PlatformMessage

Адаптер нативного сообщения платформы:

```java
public interface PlatformMessage {
    PlatformType platform();
    long chatId();
    long senderId();
    String text();
    boolean hasAttachments();
    Object nativeMessage();
}
```

> `nativeMessage()` — оригинальный объект SDK платформы
> Core его **не использует**, только передаёт обратно в `CoreResponse.CopyMessage`

### 2: MessageAdapter

Преобразует нативное сообщение платформы в `PlatformMessage`:

```java
@Component
public class TelegramMessageAdapter {
    public PlatformMessage adapt(Message message) {
        return new TelegramPlatformMessage(message);
    }
}
```

### 3: CommandAdapter

Преобразует команды администратора в `CoreCommand`:

```java
public record CoreCommand(
    PlatformType platform,
    long adminId,
    String name,
    List<String> args
) {}
```

### 4: UpdateHandler (Router)

Платформа **сама определяет контекст** сообщения:

* admin command
* admin reply
* user message

Пример логики:

```text
1. Если команда - CoreCommandProcessor
2. Если reply администратора - CoreSupportProcessor.handleAdminReply
3. Если сообщение пользователя - CoreSupportProcessor.handleUserMessage
```

### 5: Выполнение `CoreResponse`

Core возвращает **намерения**, платформа обязана их выполнить:

```java
public sealed interface CoreResponse {
    record SendText(long chatId, String text) implements CoreResponse {}
    record CopyMessage(long from, long to, Object nativeMsg) implements CoreResponse {}
    enum Ignore implements CoreResponse { INSTANCE }
}
```

Платформа знает:

* как отправить текст
* как скопировать сообщение
* как игнорировать

## Конфигурация и web панель

Все настройки находятся в `application.yml`.

Редактирование возможно:

* вручную
* через web-панель

### Web админ панель
* по умолчанию находится на порту 8080
* URL: `http://localhost:8080/`
* HTTP Basic Auth
* Позволяет редактировать **весь `application.yml`**
* Изменения вступают в силу после перезапуска


## Roadmap

- [x] Core-движок (tickets, rate-limit, ban)
- [x] Telegram платформа
- [x] Web UI для конфигурации
- [ ] Discord платформа
- [ ] Хранение rate-limit в Redis